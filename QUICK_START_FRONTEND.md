# Быстрый старт: Запуск фронтенда

## ✅ Что уже сделано:

1. **Бэкенд работает** в AWS ECS Fargate
2. **Публичный IP**: `3.28.52.168:8000`
3. **Security Group**: порт 8000 открыт
4. **Файл `.env.local`**: создан с правильным API URL

## 🚀 Запуск фронтенда:

### Вариант 1: Уже запущен (если команда выполнилась)

Откройте в браузере: **http://localhost:3000**

### Вариант 2: Запустить вручную

```bash
cd frontend
npm install  # если еще не установлено
npm run dev
```

Фронтенд будет доступен на: **http://localhost:3000**

## 📝 Использование:

1. **Главная страница**: `http://localhost:3000`
   - Список агентов
   - Создание нового чата

2. **Чат с агентом**: `http://localhost:3000/chat/[agentId]`
   - Например: `http://localhost:3000/chat/doctor_001`

3. **Админ панель**: `http://localhost:3000/admin`
   - Статистика
   - Управление агентами
   - Просмотр разговоров

## ⚙️ Настройки:

Файл `frontend/.env.local` содержит:
```
NEXT_PUBLIC_API_URL=http://3.28.52.168:8000
NEXT_PUBLIC_WS_URL=ws://3.28.52.168:8000
```

Если IP изменится (после перезапуска задачи), обновите этот файл.

## 🔍 Проверка работы:

1. Откройте `http://localhost:3000`
2. Выберите или создайте агента
3. Начните чат - сообщения должны отправляться на бэкенд в AWS

## ⚠️ Важно:

- Фронтенд работает локально, бэкенд - в AWS
- Убедитесь, что порт 8000 открыт в Security Group (уже открыт)
- Если бэкенд перезапустится, IP может измениться - обновите `.env.local`

## 🐛 Troubleshooting:

**Фронтенд не подключается к бэкенду:**
1. Проверьте, что бэкенд работает: `curl http://3.28.52.168:8000/health`
2. Проверьте `.env.local` файл
3. Проверьте консоль браузера на ошибки CORS

**CORS ошибки:**
- Убедитесь, что бэкенд разрешает запросы с `localhost:3000`
- Проверьте настройки CORS в `backend/app/main.py`






