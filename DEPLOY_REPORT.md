# Отчет о деплое и тестировании

## ✅ Деплой завершен успешно

**Дата:** 2026-01-09 15:41 UTC+3

### Backend
- ✅ Docker образ собран и загружен в ECR
- ✅ ECS сервис `doctor-agent-backend` обновлен
- ✅ Статус: ACTIVE, 1/1 tasks running
- ✅ Последний деплой: 2026-01-09T15:37:50

### Frontend
- ✅ Docker образ собран и загружен в ECR
- ✅ ECS сервис `doctor-agent-frontend` обновлен
- ✅ Статус: ACTIVE, 1/1 tasks running
- ✅ Последний деплой: 2026-01-09T15:41:49

### Сервер
- ✅ URL: http://doctor-agent-alb-1328234230.me-central-1.elb.amazonaws.com
- ✅ Health endpoint работает: `/health` возвращает `{"status": "healthy"}`
- ✅ API доступен: `/api/v1/webhook-events/recent` работает

## 📋 Что было исправлено

1. **Обработка разных типов событий Instagram webhook**
   - Добавлена функция `_get_event_type()` для определения типа события
   - Система обрабатывает только события типа `message` (обычные сообщения)
   - События `message_edit`, `message_reaction` и другие пропускаются

2. **Улучшенное извлечение ID из webhook событий**
   - `webhook_event_store.py` определяет тип события
   - ID извлекаются только для обычных сообщений
   - Для `message_edit` сохраняется только message_id

3. **Улучшенное отображение на frontend**
   - Показывается тип события
   - Для `message_edit` отображается предупреждение
   - Кнопки "Использовать ID" активны только для обычных сообщений

4. **Улучшенное логирование**
   - В логах видно тип события
   - Для не-message событий выводится предупреждение

## 🧪 Тестирование

### Текущий статус
- ✅ Сервер доступен и отвечает на запросы
- ✅ Health endpoint работает
- ✅ Webhook events API работает
- ⏳ Ожидание webhook событий от Instagram (нужно отправить сообщение)

### Как протестировать

1. **Откройте тестовую страницу:**
   ```
   http://doctor-agent-alb-1328234230.me-central-1.elb.amazonaws.com/admin/instagram-test
   ```

2. **Отправьте новое сообщение агенту в Instagram:**
   - Откройте Instagram app
   - Найдите аккаунт агента
   - Отправьте новое сообщение (не редактируйте старое!)

3. **Проверьте webhook событие:**
   - На тестовой странице должно появиться событие
   - Для обычных сообщений (`event_type='message'`) должны отображаться:
     - Sender ID (зеленым) - это recipient_id для отправки ответа
     - Recipient ID (синим) - это account_id для отправки
   - Для `message_edit` событий будет показано предупреждение

4. **Отправьте тестовое сообщение:**
   - Нажмите кнопку "Использовать Sender ID" для заполнения Recipient ID
   - Заполните Account ID (это recipient.id из webhook)
   - Нажмите "Отправить сообщение"

## 📊 Структура webhook событий

### Обычное сообщение (можно отправлять ответ):
```json
{
  "object": "instagram",
  "entry": [{
    "messaging": [{
      "sender": {"id": "USER_ID"},      // ← Это recipient_id для отправки
      "recipient": {"id": "ACCOUNT_ID"}, // ← Это account_id для отправки
      "message": {
        "text": "Текст сообщения",
        "mid": "message_id"
      }
    }]
  }]
}
```

### Редактирование сообщения (нельзя отправлять ответ):
```json
{
  "object": "instagram",
  "entry": [{
    "messaging": [{
      "message_edit": {
        "mid": "message_id",
        "num_edit": 0
      }
      // Нет sender/recipient!
    }]
  }]
}
```

## ⚠️ Важные замечания

1. **24-часовое окно ответов**: Instagram позволяет отправлять сообщения только в течение 24 часов после последнего сообщения от пользователя

2. **Тип события**: Для отправки ответа нужно обычное сообщение (`event_type='message'`), а не `message_edit` или другие типы

3. **Sender ID = Recipient ID для отправки**: `sender.id` из webhook используется как `recipient_id` при отправке ответа через API

## 🔗 Полезные ссылки

- Тестовая страница: http://doctor-agent-alb-1328234230.me-central-1.elb.amazonaws.com/admin/instagram-test
- Health check: http://doctor-agent-alb-1328234230.me-central-1.elb.amazonaws.com/health
- Webhook events API: http://doctor-agent-alb-1328234230.me-central-1.elb.amazonaws.com/api/v1/webhook-events/recent

## ✅ Итог

Все изменения успешно задеплоены. Система готова к тестированию. Отправьте новое сообщение агенту в Instagram и проверьте, что ID правильно извлекаются и отображаются на тестовой странице.

